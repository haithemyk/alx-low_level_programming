#include "main.h"
#include <stdio.h>
/**
* print_rev - print string of character reversed
* @s:the string that will be treated
*/
void print_rev(char *s)
{
int i;
i = _strlen(s) - 1;
while (i >= 0)
{
putchar(s[i]);
i--;
}
putchar('\n');
}
