#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - will print all alphabet letters
* Return: if the program works perfectly
*/
int main(void)
{
int i;
for (i = 97 ; i < 123 ; i++)
{
putchar((char) i);
}
putchar('\n');
return (0);
}
