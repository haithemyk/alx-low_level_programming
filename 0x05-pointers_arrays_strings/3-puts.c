#include "main.h"
/**
* _puts - print string of character similair to stdio lib
* @str:the string that will be treated
*/
void _puts(char *str);
{
int i;
i = 0;
while (s[i] != '\0')
{
putchar(s[i]);
i++;
}
putchar("\n");
}
