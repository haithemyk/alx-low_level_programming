#include "main.h"
#include <stdio.h>
/**
* main - will print the first argv element
* @argv:the elements of arg
* Return: return 0 means succes
*/
int main(char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
