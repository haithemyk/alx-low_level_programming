#include "main.h"
#include <stdio.h>
/**
* mynameis - will print the string s
* @argc:the count of element of array argv
* @argv:the elements of arg
* Return: return 0 means succes
*/
int mynameis(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
