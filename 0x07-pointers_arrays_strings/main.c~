#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *s = "ab5645787";
    char *f = "abcd";


    s = _strpbrk(s, f);
    printf("%s\n",s );
    return (0);
}
