#include "main.h"
/**
* _abs - absolute value of number
* @n: the number that will be checked
*
* Return:  + -->  1  - --> -1  0 --> null
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else (n <= 0)
{
return (-n);
}
}
